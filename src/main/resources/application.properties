# Application name
spring.application.name=teacher-manager

# ------------------------------
# PostgreSQL Database Configuration (Render)
# ------------------------------
SPRING_DATASOURCE_URL=jdbc:postgresql://dpg-d32titfdiees7393nb4g-a:5432/schooldb_faec
SPRING_DATASOURCE_USERNAME=schooldb_faec_user
SPRING_DATASOURCE_PASSWORD=uomnqkxt3wCAxhDI2hIpO1YQBm5dyhZF

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ------------------------------
# Server Configuration
# ------------------------------
server.port=${PORT:8080}

# ------------------------------
# Logging (optional)
# ------------------------------
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
